name: Sync Fork

on:
  schedule:
    - cron: '0 0 * * *' # every days
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v2.0
        with:
          token: ${{ secrets.FORK_SYNC }}
          owner: AngryBirdz
          repo: rdgen
          base: master
          head: master
          auto_approve: true
